import React from "react";

const AboutPage = () => (
  <div>
    <h2>About</h2>
    <p>
      This app uses React, Redux, React Router, Material UI and bootstrap as the
      main libraries.
    </p>
  </div>
);

export default AboutPage;
